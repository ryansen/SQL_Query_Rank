# PostgreSQL connection settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=queryrank
POSTGRES_USER=queryrank
POSTGRES_PASSWORD=queryrank

# Optional: full DSN overrides individual settings above
# DATABASE_URL=postgresql://queryrank:queryrank@localhost:5433/queryrank

# Do not put real API keys in committed files.
# For AI question generation, set OPENAI_API_KEY only in your local shell or .env.
# OPENAI_API_KEY=
